import 'package:flutter/material.dart';
import 'package:vigoplace1/utils/unavailable.dart';

class SearchPage extends StatelessWidget {
  const SearchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Unavailable();
  }
}
